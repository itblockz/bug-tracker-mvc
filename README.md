# Bug Tracking System

> **แบบฝึกหัดการเขียนโปรแกรมแบบ MVC**  
> ตัวอย่างสำหรับการเรียนรู้ MVC Design Pattern ด้วย Flask

ระบบติดตามบั๊กซอฟต์แวร์ (Bug Tracking System) พัฒนาด้วย **Flask** และ **MVC Design Pattern**

---

## a. โครงสร้าง MVC

### ไฟล์และหน้าที่

| ไฟล์ | หน้าที่ใน MVC | รายละเอียด |
|------|---------------|-------------|
| `models.py` | **Model** | กำหนดโครงสร้างข้อมูล (User, Bug, BugReport) และเชื่อมต่อฐานข้อมูล SQLite |
| `app.py` | **Controller** | รับ request, ประมวลผล business logic, ส่งข้อมูลให้ View |
| `templates/*.html` | **View** | แสดงผลหน้าจอ UI ด้วย Jinja2 template |

### การทำงานร่วมกัน

```
[User Request] → [Controller (app.py)]
                      ↓
              [Model (models.py)] ←→ [Database (SQLite)]
                      ↓
              [View (templates/)]
                      ↓
              [HTML Response]
```

1. **User** ส่ง request มาที่ URL (เช่น `/`, `/bug/10000001`)
2. **Controller** (`app.py`) รับ request และเรียกใช้ Model เพื่อดึง/บันทึกข้อมูล
3. **Model** (`models.py`) query ข้อมูลจาก SQLite database
4. **Controller** ส่งข้อมูลไปให้ **View** (`templates/*.html`)
5. **View** render HTML และส่งกลับไปให้ User

---

## b. สรุป Routes/Actions หลัก

| Route | Method | หน้าที่ | สิทธิ์ |
|-------|--------|---------|--------|
| `/` | GET | หน้ารวมบั๊ก (เรียงลำดับได้) | ทุกคน |
| `/login` | GET, POST | เข้าสู่ระบบ | ทุกคน |
| `/logout` | GET | ออกจากระบบ | ทุกคน |
| `/bug/<id>` | GET | หน้ารายละเอียดบั๊ก | ทุกคน |
| `/bug/<id>/report` | POST | รายงานบั๊ก | user (login) |
| `/bug/<id>/verify` | POST | ตรวจสอบบั๊ก | developer |
| `/summary` | GET | หน้าสรุปผล | ทุกคน |

---

## หน้าจอ View สำคัญ

| ไฟล์ | หน้าจอ | ฟังก์ชันหลัก |
|------|--------|--------------|
| `index.html` | หน้ารวมบั๊ก | แสดงบั๊กทั้งหมด, เรียงลำดับ |
| `bug_detail.html` | หน้ารายละเอียด | แสดงข้อมูล, รายงาน, ตรวจสอบ |
| `summary.html` | หน้าสรุปผล | บั๊ก critical, บั๊กที่ verified |
| `login.html` | หน้า login | เข้าสู่ระบบด้วย ID |

---

## Business Rules

1. ผู้ใช้รายงานบั๊กเดียวกันซ้ำไม่ได้
2. รายงาน ≥ 3 ครั้ง → สถานะเปลี่ยนเป็น "critical"
3. บั๊กที่ตรวจสอบแล้ว → รายงานเพิ่มไม่ได้
4. นักพัฒนา (developer) รายงานบั๊กไม่ได้ แต่ตรวจสอบได้

---

## วิธีรัน

```bash
# ติดตั้ง dependencies
pip install flask flask-sqlalchemy

# สร้างข้อมูลตัวอย่าง
python seed.py

# รันแอป
python app.py
```

เปิด browser ไปที่ `http://127.0.0.1:5000`

---

## ข้อมูลตัวอย่าง

| ประเภท | จำนวน |
|--------|-------|
| Users | 10 คน (9 user + 1 developer) |
| Bugs | 8 รายการ (4 normal + 4 critical) |
| BugReports | 15 รายงาน |

### User IDs สำหรับ Login

- User ทั่วไป: `1` - `9`
- นักพัฒนา: `10`

---

## 📚 แหล่งเรียนรู้เพิ่มเติม

- [Flask Documentation](https://flask.palletsprojects.com/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)
- [MVC Design Pattern](https://en.wikipedia.org/wiki/Model%E2%80%93view%E2%80%93controller)

---

*แบบฝึกหัดนี้สร้างขึ้นเพื่อการเรียนรู้ MVC Pattern เท่านั้น*
